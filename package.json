{
  "name": "creatorlend",
  "version": "0.1.0",
  "private": true,
  "description": "Faire Vergütungsplattform für Kreative – ein Werk = eine Woche = eine faire Bezahlung.",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
